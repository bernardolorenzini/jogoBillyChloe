//
//  types.h
//  Sprites
//
//  Created by Bernardo Lorenzini on 20/07/23.
//

#pragma once

#include <stddef.h>
#include <inttypes.h>
#include "global.h"

//changind name of unigned int variables (8 bits, 16 bits, 32 bits, 64 bits) to make it easier to type
typedef uint8_t  ui8;
typedef uint16_t ui16;
typedef uint32_t ui32;
typedef uint64_t ui64;

//changind name of int variables (8 bits, 16 bits, 32 bits, 64 bits) to make it easier to type
typedef int8_t  i8;
typedef int16_t i16;
typedef int32_t i32;
typedef int64_t i64;


//changind name of float variables (32 bits, 64 bits) to make it easier to type
typedef float   f32;
typedef double  f64;

//changind name size variables
typedef size_t  usize;
typedef size_t usize;